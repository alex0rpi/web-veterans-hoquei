import { MainContent } from '../components';
import { MainLayout } from '../layouts/Layout';

export const MainPage = () => {
  return (
    <MainLayout>
      <MainContent />
    </MainLayout>
  );
};
