import { AdminContent } from '../components';
import { MainLayout } from '../layouts/Layout';

export const AdminPage = () => {
  return (
    <MainLayout>
      <AdminContent />
    </MainLayout>
  );
};
